# wrangler.toml — root of repo
name = "meyou-pipeline"
main = "worker/worker.js"   # <-- adjust ONLY if your worker file lives elsewhere
compatibility_date = "2024-09-10"

# Uncomment and fill if you prefer explicit account binding here
# account_id = "YOUR_ACCOUNT_ID"
