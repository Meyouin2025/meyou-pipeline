name = "meyou-gateway"          # <- paste the EXACT slug here
main = "worker.js"
compatibility_date = "2025-11-01"

routes = [
  { pattern = "api.meyoustudios.com/*", zone_name = "meyoustudios.com" }
]
